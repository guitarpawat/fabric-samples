extends layout.pug

block content
    form(action='/closebid' method='post')
        
        .form-group
            p Issuer
            input.input(type='text' name='issuer')
        .form-group
            p Paper Number
            input.input(type='text' name='papernum')
        .form-group
            p Owner
            input.input(type='text' name='currentowner' value=id readonly)
        p
        button(type='submit') Close Bid